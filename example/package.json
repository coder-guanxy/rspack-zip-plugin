{
  "name": "rspack-zip-plugin-example",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "rspack serve",
    "build": "rspack build"
  },
  "devDependencies": {
    "@rspack/cli": "latest",
    "rspack-zip-plugin": "^0.0.2"
  }
}
